debug: 
  false

server:
  servlet:
    context-path: /api
 
spring:
  application: 
    name:  api
  devtools:
    restart:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true 
    format_sql: true
      
  h2:
    console:
      enabled: true
      path: /h2/console